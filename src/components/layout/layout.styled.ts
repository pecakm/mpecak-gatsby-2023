import styled from 'styled-components';
import { EdgeMargin } from '../../styles/edge.enum';

export const Container = styled.div`
  font-family: 'Roboto';
`;

export const PageWrapper = styled.main`
  padding: 40px ${EdgeMargin.ExtraSmall};
`;
